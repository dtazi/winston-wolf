# Azure AD app credentials for the Outreach OAuth flow.
# These are public identifiers (not secrets) — they appear in OAuth URLs by design.
# Real values for Richbond live in .env (gitignored).

AZURE_CLIENT_ID=
AZURE_TENANT_ID=
